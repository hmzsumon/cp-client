export type Activity = {
  id: string;
  title: string;
  date: string;
  amount: number;
  currency?: string;
};
export type QuickPair = {
  id: string;
  symbol: string;
  price: string;
  change: string;
};
